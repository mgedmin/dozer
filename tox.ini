[tox]
envlist = py27, py33, py34, py35, py36

[testenv]
deps =
    Pillow
    WebTest
    mock
    nose
commands =
    nosetests {posargs}

[testenv:py]
commands =
    python --version
    nosetests {posargs}
